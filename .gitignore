# Secrets
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Pipeline run outputs (per-run artifacts; the manifest in out/ stays tracked)
work/

# Node / Netlify CLI
node_modules/
.netlify/

# OS / editor
.DS_Store
.idea/
.vscode/

# Claude Code per-machine settings (project-wide settings.json stays tracked)
.claude/settings.local.json

# Claude Code sub-agent worktrees (transient; never tracked)
.claude/worktrees/
